{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "sliderule": {
      "type": "remote",
      "url": "https://mcp.slideruleearth.io/sliderule",
      "enabled": true,
      "headers": {
        "Authorization": "Bearer {env:SLIDERULE_MCP_TOKEN}"
      }
    }
  }
}
