type: object
properties:
  type:
    type: string
    enum: [FeatureCollection]
  hits:
    type: integer
    description: Total number of matching features
  features:
    type: array
    items:
      $ref: './3DEP1MFeature.yml'
required:
  - type
  - hits
  - features
