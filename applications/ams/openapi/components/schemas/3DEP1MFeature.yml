type: object
properties:
  type:
    type: string
    enum: [Feature]
  id:
    type: string
    description: Granule ID
  geometry:
    type: object
    properties:
      type:
        type: string
        enum: [Polygon]
      coordinates:
        type: array
        items:
          type: array
          items:
            type: array
            items:
              type: number
  properties:
    type: object
    properties:
      datetime:
        type: string
        format: date-time
      url:
        type: string
        description: URL to the elevation data asset
