# conda env create -f environment.yml
name: mcp
channels:
  - conda-forge
dependencies:
  - boto3
  - coverage
  - geopandas
  - pytest
  - pytest-env
  - requests
  - sliderule
  - trafilatura
  - pip