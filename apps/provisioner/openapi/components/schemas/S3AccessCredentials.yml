type: object
properties:
  access_key_id:
    type: string
    description: AWS_ACCESS_KEY_ID
  secret_access_key:
    type: string
    description: AWS_SECRET_ACCESS_KEY
  session_token:
    type: string
    description: AWS_SESSION_TOKEN
  expiration:
    type: string
    description: AWS_CREDENTIAL_EXPIRATION
