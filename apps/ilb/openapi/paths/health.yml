get:
  summary: Health check
  description: Returns health status of the orchestrator.
  operationId: health
  tags:
    - Internal
  responses:
    '200':
      description: Service is healthy.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/HealthResponse.yml'
